#숫자를 입력받기 기능 => n이라고한다
#1부터 n까지 반복 =>
#반복하면서 나오는 숫자가 짝수인지 검사
#합 구하기 합을 저장하는 변수를 만들자 변수 sum
# sum 을 누적해서 저장
n = int(input("숫자를 입력"))
sum = 0
for s in range(1,n+1):
  if s % 2 == 0:
    sum = sum + s #sum 에 s를 누적한다는것은 현재 sum에 k를 추가 sum에 보관
    

#반복이끝났다 
print(sum)
